{
  "name": "devcontainer-backend-doctor-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config site/vite.config.ts",
    "test": "cargo test --workspace && node --test site/tests/*.test.mjs",
    "build:site": "vite build --config site/vite.config.ts",
    "build:cli": "cargo build --release --workspace && mkdir -p dist/bin && cp target/release/devcontainer-backend-doctor dist/bin/",
    "build": "npm run build:site && npm run build:cli",
    "test:browser": "playwright test",
    "check": "cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && npm test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
